#pragma once
#include"../dllHeader.h"
#include<cmath>

static float Abs(float f) {
	return std::abs(f);
}

static float Acos(float f) {
	return std::acos(f);
}

static float Sqrt(float f) {
	return std::sqrt(f);
}

float epsilon = 0.0000000596f;

float PI = 3.14159265359f;